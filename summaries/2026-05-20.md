# llama.cpp daily summary — 2026-05-20

Today saw 22 commits merged to upstream/master, with notable activity across GPU backends (OpenCL, Hexagon, WebGPU, SYCL), a significant MTP speculative-decoding clean-up, and several server and tooling improvements. No breaking changes were flagged, but the MTP rework touches core speculative-decoding logic and the RPC graph-UID fix addresses a correctness bug introduced by MTP.

## OpenCL / Qualcomm Adreno

- **MoE support for q4\_k, q5\_k, q6\_k** on Adreno via new GEMM/GEMV kernels and a CVT helper (~2,600 lines of new kernel code) — #23303 `b28a2f3`

## Hexagon / HTP

- Add **MROPE and IMROPE** support to the HTP rope op — #23317 `17d22a3`
- Enable **NORM op** on HTP backend — #23319 `ac76808`

## WebGPU

- Extend **Gated Delta Net (GDN)** shader to support `K > 1` — #23299 `c85a242`

## SYCL / Intel

- Add `GGML_SYCL_USE_ASYNC_MEM_OP` environment toggle to control async memory operations; async is now forced on when SYCL graphs are explicitly enabled — #22153 `439f1b1`
- Add performance reference section to `SYCL.md` docs — #23315 `aabee04`

## MTP / Speculative Decoding

- **MTP clean-up** (Georgi Gerganov): re-enables `p-min` with MTP drafts, re-enables ngram spec with recurrent-state rollback, fixes ngram-map params and acceptance logic for combined ngram + draft configs, fixes graph reuse for mixed `token`+`embd` batches, and extends `--spec-default` with `ngram-map-k4v` — #23269 `d14ce3d`

## RPC

- **Bug fix**: move `last_graph_uid` into device context so it is updated correctly when multiple compute contexts share the same RPC device (regression introduced with MTP) — #23273 `c3e9ade`

## API / Server

- **Bug fix**: guarantee at least 1 token to decode in server-context slot handling — #23280 `ccee426`
- Print **graphs-reused** counter in slot timing logs — #23279 `3c81c8d`

## UI

- Chat Screen refactor: extract `ChatScreenGreeting`, `ChatScreenServerError`, `ChatScreenProcessingInfo`, `ChatScreenActionScrollDown` components — #23333 `67ace02`
- Bump UI packages and fix build warnings — #23300 `6db1304`

## Eval / Testing

- `llama-eval`: add per-task summary stats, tabbed HTML report layout, color-coded correctness, and improved simulator realism — #23151 `d2e179a`
- `save-load-state`: refactor tests for clarity (each test creates its own context) — #23196 `cd963fe`

## Build / CI

- Install `libssl-dev` in CI — #23325 `4b262ab`
- Install KleidiAI runner dependencies for server CI — #23259 `00c461c`

## Convert / Misc

- Filter LoRA tensor names during conversion — #23077 `f1c1c5c`
- Update MTP-related help text in `convert_hf_to_gguf.py` — #23334 `b7393a4`
- Fix `--help` text and `--fit` verbosity behavior — #23278 `57cb35c`, #23282 `7256fce`
- Add `--log-file` hint to GitHub issue templates — #23277 `a807867`

---

**PRs closed due to improper AI policy usage:** 0 (none identified from the commit log in the last 24 hours).
