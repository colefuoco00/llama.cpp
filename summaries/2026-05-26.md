# llama.cpp daily summary — 2026-05-26

A high-activity day with 26 commits touching CUDA, Metal, server/API, ggml internals, CI infrastructure, and model correctness. The ggml library was bumped to version 0.13.0. A notable performance regression fix for Nemotron-H restores throughput from 64.9 to 103 t/s.

## CUDA / GPU

- **New feature**: Fast Walsh-Hadamard Transform (FWHT) kernel added to CUDA backend — enables efficient spectral operations on GPU (#23615, `c1f1e28`)
- **Performance**: Parallelize quant LUT initialization (`iq2xs`/`iq3xs`) using OpenMP; moves OpenMP detection from ggml-cpu to ggml-base (#23595, `826539c`)

## Metal

- Add Apple SoC/device ID detection at backend init, populating `ggml-metal-device.h` with known device identifiers (#23566, `35c9b1f`)

## Model Fixes

- **Performance regression fix**: Tag `ffn_latent` tensors as `MUL_MAT` (was incorrectly `MUL`) to fix buffer probe on Nemotron-H; restores ~103 t/s from ~65 t/s on 120B Q5_K_M (#23664, `328874d`)
- Server: MTP layer kv-cache now correctly respects the draft tensor type (`ctk`) (#23646, `6c4cbdc`)

## API / Server

- **Server checkpoint fix**: Smarter checkpoint creation — extracts message spans from chat templates to locate the pre-user-input prompt boundary, avoids mid-prompt periodic checkpoints, adds `--checkpoint-min-step` flag (#22929, `e2ef8fe`)
- UI: Fix stop/continue behavior during an agentic loop (#23356, `5a4126a`)
- UI: Media attachments now appear before text in outgoing messages (#23467, `b964876`)

## ggml

- Bump ggml to v0.13.0 (`45158f4`) — includes v0.12.1 patch (`ce5890b`)
- New API: `gguf_init_from_callback` and `gguf_init_from_buffer` for flexible GGUF loading (#22341, `66efd13`)
- Fix out-of-bounds read in `ggml_dyn_tallocr_remove_block` (`fa97041`)
- Fix ggml context size calculation and memory leak in tensor-parallel (TP) mode (#22616, `ae251b5`)
- Fix docstring for `ggml_silu_back` args (`b251f74`)

## Convert / Quantization

- Add compressed-tensors NVFP4 conversion support in `convert`; also moves Qwen MTP tensor remap into `filter_tensors` (#21095, `a4d2d4a`)

## CI / Build

- Major CI restructuring: split Android, HIP, WebGPU, RPC, RISC-V, IBM, OpenCL, and Snapdragon into dedicated workflow files; prune backend-unrelated jobs from PR triggers (#23675, `302e2c2`)
- Move most "slim" CI jobs to self-hosted runners; add dedicated `ui-self-hosted.yml` workflow (#23619, `28123a3`)
- Snapdragon: bump Docker toolchain to v0.7 to fix UI build failures (#23680, `4bead4e`)
- SpacemiT: update toolchain URL and add `-L` flag to curl for redirect support (#23642, `5fdf07e`)
- Fix pre-tokenizer-hashes CI check (#23651, `062d311`)
- Install host compiler in Android NDK build job (#23630, `d55fb97`)

## Vendor / Misc

- Update `cpp-httplib` to v0.45.1 (#23639, `9627d0f`)
- Fix additional integer overflows in `perplexity` tool (#23623, `6d57c26`)
- Document that only one on-device KV state can be saved per sequence (#23520, `314e729`)

---

## AI Policy Report

**PRs closed for improper AI usage (last 24 hours): 1**

- **#23649** ("Audio max"): Opened and closed within one minute; PR body was empty (template only) with no meaningful human-authored content — closed as an AI-generated submission per project policy.

*(PR #23638 also disclosed AI assistance and was closed after reviewer discussion, but appears to have involved genuine human understanding of the changes.)*
