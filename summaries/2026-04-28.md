# llama.cpp daily summary — 2026-04-28

Today saw 11 commits land on upstream/master, with significant activity in WebGPU backend expansion, server/router improvements, and a variety of model correctness and build fixes. No breaking changes were identified, though the aligned tile buffer change and duplicate scale removal are worth verifying in downstream integrations.

## WebGPU

- Add Q1_0 quantization support: fast matmul + mat-vec kernels with WGSL shaders (#22374, `434b2a1`)
- Add fast mat-vec kernels for i-quants (IQ2/IQ3/IQ4 families), 543 lines of new WGSL (#22344, `665abc6`)

## API / Server

- Router now forwards multipart form-data to model server, fixing `/v1/audio/transcriptions` in router mode (#22118, `983ca89`)

## Model Fixes

- Remove duplicate `wo_s` scale emission after `build_attn` for Qwen3, Qwen3-MoE, and LLaMA models — correctness fix (#22421, `0f1bb60`)

## Performance / CPU

- Use 64-byte aligned tile buffers in AMX `mmq.cpp`; benchmarks show up to ~2% pp512 speedup on IQ4_NL and Q4_0 quantizations (#21058, `f84270e`)

## RPC / Platform

- Fix RPC-server KV cache path construction failing on Windows (directory creation + path separator issue) (#22394, `ceaf47c`)

## Conversion

- Remove `input_scale` handling for dequantized FP8 modelopt checkpoints in `convert_hf_to_gguf.py` (#22356, `d13540b`)

## Build / Common

- Fix missing symbol exports in `llama-common`; refactor `common_debug_cb_user_data` to use pimpl, reducing transitive header dependencies (#22340, `5594d13`)

## Tests

- Add Gemma4 chat-template edge-case parsing tests (#22420, `4414c04`)

## Misc

- Prefer `q8_0` quantization when `q4_k` is unavailable during model download (`e940b3d`)
- Fix integer type cast in unaccounted memory calculation in `fit.cpp` (#22424, `42401c7`)

---

**PRs closed due to improper AI policy usage:** 0
