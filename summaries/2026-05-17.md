# llama.cpp daily summary — 2026-05-17

Today was a significant day with 17 commits landed, headlined by a large MTP (Multi-Token Prediction) speculative decoding feature and a notable ggml version bump to 0.12.0. Activity spanned server internals, WebUI, Metal, and model quantization.

## Speculative Decoding / Core

- **MTP (Multi-Token Prediction) support** for Qwen3.5/Qwen3.5-MoE models via recurrent-state checkpointing; adds partial KV rollback across CPU, CUDA, Metal, and Vulkan backends, plus a new `test-recurrent-state-rollback` test — PR #22673, SHA `2555826`. This is a major new feature enabling draft-free speculative decoding on recurrent architectures.

## Model / Quantization

- **NvFP4 quantized LM head support**: adds NVIDIA FP4 quantization for the language model head, with assert guards for tied-embedding cases; touches all ~100 model files — PR #23046, SHA `42928bc`.

## ggml / Build

- **ggml version bump to 0.12.0** — SHA `e6c37a1` (ggml/1494).
- **Fix pkgconfig install path**: `ggml.pc` now installs to `<libdir>/pkgconfig` per convention — SHA `2eb3e6b` (ggml/1480).
- **ggml sync** from upstream ggml — SHA `3a92bc9`.

## Metal

- **`kernel_conv_transpose_1d` loop tightening**: analytically computes valid input-position bounds instead of iterating the full IL range; reported ~3–4x faster codec decode on Apple M1 with zero GPU watchdog hits — SHA `560445b` (ggml/1477).

## API / Server

- **Router mode: heap-allocate tmp buffer** to avoid stack overflow — PR #23159, SHA `b64739e`.
- **Router mode: skip device enumeration** to prevent creation of CUDA primary context on startup — PR #23137, SHA `64b38b5`.

## WebUI / UI

- **Video file input**: WebUI now accepts video attachments with thumbnail preview and model-capability gating — PR #22830, SHA `4f13cb7`.
- **MCP tool call timeout**: new configurable timeout setting for MCP tool calls via a dedicated Settings tab — PR #23138, SHA `0253fb2`.
- **Fix MCP resource template parameter handling** in URI template utility — PR #23117, SHA `b81c2cd`.
- **Fix reactive loop** in settings sync props effect — PR #23127, SHA `366c5e2`.
- **Accessibility fixes** in ChatFormActionAdd menu trigger/items — PR #22736, SHA `1428004`.

## Vendor / Misc

- **Update cpp-httplib to 0.45.0** — PR #23103, SHA `18675b6`.
- **Fix UI publish workflow**: add missing build step — PR #23134, SHA `1d9f99a`.
- **Vulkan**: remove duplicate `#include <memory>` — PR #23144, SHA `6049906`.
- **Docs**: fix stale relative links in `tools/ui/README.md` — PR #23139, SHA `25b1bc9`.

---

**PRs closed due to improper AI policy usage:** 0
