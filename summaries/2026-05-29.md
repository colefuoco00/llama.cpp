# llama.cpp daily summary — 2026-05-29

A busy day with 31 commits touching GPU backends (CUDA, Vulkan, Hexagon, OpenCL), multimodal fixes, CI refactoring, and new conversion tooling. Notable highlights include op fusion on Hexagon, a fast Walsh-Hadamard Transform path in Vulkan, FP8→Q8 conversion support, and a CUDA integer-overflow bugfix in the flash-attention MMA kernel.

## CUDA / GPU

- **Fix KQ mask integer overflow in fattn MMA kernel** — prevents corruption on long contexts with large ubatch sizes (#23610, `09e7b76`)
- **Route batch≥4 quantized matmul to MMQ on AMD MFMA hardware** — improves throughput on RDNA/CDNA GPUs that support tiled GEMM (#23227, `bc81d47`)
- **Add MMVQ Turing parameters** — new `MMVQ_PARAMETERS_TURING` entry for SM75 (GTX 16xx) (#23729, `d7be461`)
- **Auto-apply iGPU flag for CUDA/HIP integrated devices** — avoids manual `GGML_BACKEND_DEVICE_TYPE_IGPU` configuration (#23007, `30af6e2`)

## Vulkan

- **Fast path for Walsh-Hadamard Transform** — dedicated compute shader, significant speedup over the generic path (#23687, `48e7078`)
- **Fix wrong index variable in inner loop** — correctness bug in source-offset computation (#23665, `7c48fb8`)
- **Fix memory logger unsafe iterator access** — race-free map iteration (#23667, `91eb8f4`)

## Hexagon (Qualcomm DSP)

- **Op fusion infrastructure + RMS_NORM×MUL fusion** — adds generic fusion support to the HTP backend, RMS_NORM+MUL as first use-case (~500 LOC refactor) (#23835, `19e92c3`)
- **HMX FA and MM refresh** — performance pass on HMX flash-attention and matrix-multiply (#23796, `a919001`)
- **OP_GATED_DELTA_NET K>1 support** — state snapshot support for K>1 sequences (#23531, `939a7dd`)

## OpenCL

- **OP_GATED_DELTA_NET kernel** — full OpenCL implementation of the gated delta-net operator, with K>1 state snapshot support (#23312, `8ad8aef`)
- **Refactor backend info printing into its own function** (#23702, `408ae2b`)

## Arm CPU

- **Fix SVE accumulation bug in vec.h/vec.cpp** — was accumulating to F16 instead of F32, causing precision loss on Arm SVE hardware (#22841, `e31cdaa`)

## Multimodal (mtmd)

- **Fix Gemma 4 projector pre_norm** — uses post-norm in early pre-release weights, now handled correctly (#23822, `c8914ad`)
- **Fix Gemma 4 audio RMS norm eps** — wrong epsilon value from upstream (#23815, `d6be315`)
- **GQA support in build_vit** — `n_head_kv` now defaults to `n_head` when not specified (#23782, `0b56d28`)
- **mtmd-debug: color and rainbow visualization modes** — debugging aid for multimodal embeddings (#23829, `751ebd1`)

## API / Server

- **HTTP ETag support in llama-server** — allows browsers/clients to cache static UI assets and skip redundant downloads (#23701, `d205df6`)
- **LLAMA_ARG_API_KEY_FILE env var** — lets `--api-key-file` be set via environment (#23167, `7fb1e70`)
- **Minor C++ modernization in server** — wider use of standard C++ features over legacy patterns (#23785, `0b24686`)

## Convert / Model

- **FP8→Q8 conversion in convert_hf_to_gguf.py** — new `--fp8-as-q8` flag; useful for FP8-quantized checkpoints (#23250, `c522908`)
- **Granite 4.1 chat template** — detection, C++ handler, and Jinja template (#23518, `bb771cb`)

## Build / CI

- **Major CI workflow refactor** — splits CUDA into separate Ubuntu/Windows workflows, renames `build.yml`→`build-cpu.yml`, improves cache key strategy, moves release trigger to ubuntu-slim (#23789, `491c4d7`)
- **Vulkan CI builds switched to Release mode** — reduces ccache bloat (#23820, `dd15579`)
- **UI release builds moved to GitHub-hosted runners** — security and performance improvement (#23823, `445b7ce`)
- **ZenDNN Dockerfile added** — new `.devops/zendnn.Dockerfile` for IBM ZenDNN accelerator (#23716, `e8d2567`)

## Misc / Fixes

- **Improve `llama help` output** (#23805, `479a9a1`)
- **Fix perplexity LOG_ERR format specifier** (#23788, `48e7eae`)
- **Fix test-llama-archs table formatting** (#23810, `d374e71`)

---

## AI Policy Violations

**2 PRs** were closed within the last 24 hours for apparent violation of the project's AI-generated content policy:

- **PR #23806** ("EngineeringQuality: Improve code engineering quality and maintainability") — opened and closed in ~5 minutes; body was essentially empty despite spanning 16 different component labels across the entire codebase. Strongly indicative of an automated/AI-generated submission.
- **PR #23807** ("jinja: improve_code_performance, real move construction.") — by the same contributor, closed within 13 minutes with an empty PR description. Same pattern.

Both were submitted by the same first-time contributor and share the hallmarks the project's AGENTS.md explicitly prohibits: automated commits, no meaningful description, and scope beyond what any single contributor could reasonably understand.
