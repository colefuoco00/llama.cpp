# llama.cpp daily summary — 2026-05-10

A SYCL-heavy day with 9 commits merged. Intel GPU work dominates: four distinct SYCL improvements land covering performance, BF16 correctness, and Battlemage AOT build support. A new model architecture (Sarvam MoE), CUDA flash attention expansion for MiMo-V2.5, and routine build/infra updates round out the day.

## CUDA/GPU

- **Flash attention MMA/Tiles for MiMo-V2.5**: adds d_kq=192 / d_v=128 dimension support to both MMA and tile-based FA paths, including GQA handling and backend ops test coverage — #22812 `046e284`

## SYCL

- **Reduce FA allocation overhead**: refactors KV buffer management into dedicated `fattn-buffers.hpp/cpp`, avoiding repeated allocations during flash attention — #22732 `e20b839`
- **BF16 support in GET_ROWS**: fixes a performance regression where BF16 embedding tensors (e.g., Gemma4's per-layer token embeddings) fell back to CPU, causing a full GPU-to-CPU transfer every token — #21391 `fd89556`
- **Q5_K reorder MMVQ/dequant + Q8_0 reorder MMVQ**: adds optimized matrix-vector quantized paths for Q5_K and Q8_0 on SYCL — #22152 `6048993`
- **Battlemage AOT build via spir64_gen**: enables Ahead-of-Time compilation for Intel Battlemage GPUs; adds MMQ subgroup annotations to guide compiler scheduling — #22147 `4a4f819`

## Models / Conversion

- **New architecture: Sarvam MoE**: adds `sarvam_moe` model support to `convert_hf_to_gguf.py` and the vocabulary layer — #20275 `1e5ad35`

## Build / Infra

- **BoringSSL bump to 0.20260508.0** in cpp-httplib CMake vendoring — #22839 `5757c4d`
- **Docker: Intel compute-runtime upgrade** to latest version in `intel.Dockerfile` — #22567 `00d56b1`
- **Nix: update flake.lock and .devops/nix/package.nix** — #22869 `65d7a8b`

---

## AI Policy Violations

Unable to determine the count of PRs closed for improper AI usage in the last 24 hours — GitHub MCP tool access is scoped to the fork (`am17an/llama.cpp`) and cannot query the upstream `ggml-org/llama.cpp` repository directly.
