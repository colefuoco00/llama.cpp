# llama.cpp daily summary — 2026-05-18

A moderately active day with 14 commits, skewing heavily toward Vulkan backend improvements and build system cleanup. The most notable change is a multi-token prediction (MTP) performance optimization that avoids logit copies during prompt decode. No breaking changes detected.

## CUDA/GPU

- **Support `d_conv=15` in `ssm-conv.cu`** — extends SSM convolution kernel to handle an additional conv dimension size needed by certain Mamba-style models (#23017, `726704a`)
- **Fix missing `cuda/iterator` include in `top-k.cu`** — follow-up to #22936 that missed one inclusion site (#23102, `84c6782`)

## Vulkan

- **Fuse SSM_CONV + BIAS + SILU into a single pass** — reduces kernel launch overhead for Mamba-style models on Vulkan (#22653, `3fbadb0`)
- **Support unaligned tensors for ROPE** — fixes correctness for non-aligned tensor strides; adds test coverage (#22637, `7ba22c6`)
- **Add BF16 → F32 copy pipelines** — fills a gap in Vulkan's type-conversion shader coverage (#22677, `fcae601`)
- **Add SPIRV-Headers cmake check** — fails configuration early when the header dependency is missing, instead of failing mid-build; also fixes CI search path on macOS (#22009, `a6d6183`)

## Core / Inference

- **Avoid logit copy during MTP prompt decode** — `llama-context` now passes logits by reference through the speculative path, removing an unnecessary allocation per decode step; notable perf win for MTP workflows (#23198, `3e12fbd`)

## API / Server

- **Delegate assistant continuation to template handlers** — refactors chat-template logic so the underlying template (not the server) owns assistant prefill; also adds `echo` parameter to exclude assistant prefill from the response (#23089, `39cf5d6`)
- **Enable streaming JSON argument values** — simplifies the PEG parser by removing per-argument atomicity, allowing argument values to stream incrementally (#23173, `f4cc787`)
- **Honor `--embd-normalize` in `llama-server`** — the flag was previously ignored by the server binary; `/embedding` now respects both the CLI default and per-request overrides (#23125, `1a68ec9`)

## Build

- **Fix `LLAMA_BUILD_UI` cmake logic** — corrects condition and removes dead code in the UI build path (#23190, `8758904`)
- **Do not install conversion script** — removes the Python conversion helper from cmake install targets (#23204, `e0de4c2`)
- **Do not check for bin install dir** — removes an unnecessary directory check from the cmake install config (#23234, `dd7cad7`)

## Misc

- **Reduce noisy ngram logs** — lowers verbosity of speculative-decoding ngram-cache diagnostics (#23185, `a16cce8`)

---

**AI policy usage report:** 0 PRs closed for improper AI usage in this period. One PR (#23017) proactively declared `AI-usage: none`, which is the expected disclosure pattern.
