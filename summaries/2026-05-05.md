# llama.cpp daily summary — 2026-05-05

A busy day dominated by a landmark architectural refactor of the model loading system, alongside notable server feature additions and GPU backend improvements across CUDA, Vulkan, and WebGPU.

## Model / Architecture

- **Major model loader refactor** (#22004, `994118a`): `load_hparams` and `load_tensors` moved to per-model definitions, touching 129 files across all model backends. New model files added (e.g. `mamba2.cpp`, `minicpm.cpp`, `nomic-bert.cpp`, `phimoe.cpp`, `hunyuan-vl.cpp`). Significant structural change — may affect downstream integrations.
- **Diffusion example refactor** (#22590, `d8794ee`): Splits diffusion generation logic into `diffusion.cpp`/`diffusion.h`, cleaning up the CLI entry point.

## API / Server

- **`/models?reload=1` endpoint** (#21848, `935a340`): New query parameter forces model reload without restarting the server — useful for hot-swapping models in production.
- **`get_datetime` server tool** (#22649, `c84e6d6`): Adds a simple built-in tool that returns the current date/time, callable by the model during inference.
- **WebUI: restore missing settings** (#22666, `fa8feae`): Settings that regressed in a previous bundle update are restored.
- **WebUI: circular dependency fix** (#22625, `36a694c`): Fixes a TypeScript circular import between `chat.service.ts` and `models.svelte.ts`.

## Inference / Parsing

- **Autoparser fixes** (#22654, `a4701c9`): Corrects newline handling and forced tool call behavior in `common/autoparser`, with extensive new tests.

## CUDA / GPU

- **CUDA `get_rows` fastdiv** (#22650, `e77056f`): Uses fast integer division for batch index splitting in `get_rows`, improving GPU throughput.

## Vulkan / WebGPU

- **Vulkan dead code removal** (#22621, `6dcd824`): Deletes unused `GGML_VK_MAX_NODES` definition.
- **WebGPU layer norm ops** (#22406, `d4b0c22`): Adds RMS norm and group norm WGSL shaders to the WebGPU backend.

## Build / Deps

- **KleidiAI v1.24.0** (#22549, `eff0670`): Updates ARM KleidiAI GEMM library to v1.24.0 using the release archive instead of a git clone.

## Docs

- **Speculative decoding docs** (#22539, `846262d`): Updates parameter names in `docs/speculative.md` and README files to match the post-refactor naming scheme (`--spec-draft-n-max`, etc.).

---

## AI Policy Report

Based on searches of PRs closed on 2026-05-04, approximately **3 PRs** appear to have been closed for improper AI-generated content:

- **#22662** ("Claude/sdxl turbo gguf conversion xq v vm"): Opened and closed within ~1 minute; empty body with "Claude/" in the title, strongly indicating an automated AI submission.
- **#22145** and **#22151** (both by `jinweihan-ai`): Author explicitly disclosed that "an agent helped surface the candidate issue, drafted the patch, and wrote this description" / "Patch drafting and PR description were AI-assisted" — both closed after ~2 weeks open, consistent with maintainer rejection under the AI policy.

Note: PR #22694 may also qualify but could not be confirmed without comment access.
