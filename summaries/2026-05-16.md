# llama.cpp daily summary — 2026-05-16

A busy day with 15 commits covering significant structural refactors to the UI and model conversion tooling, multimodal improvements, server compatibility fixes, and test coverage additions.

## UI / Server

- **UI folder restructure** (`tools/ui`): Renamed the entire web UI from `webui` to `ui` throughout the codebase — CMake variables (`LLAMA_BUILD_UI`), CLI flags (`--ui`, `--ui-config`), env vars, and C++ struct fields. Old names kept as deprecated aliases for backward compatibility. (#23064, `59778f0`)
- **WebUI theme fix**: Theme from `--webui-config-file` (now `--ui-config-file`) was not applied on first load with a fresh `localStorage`; fixed. (#22902, `8be1786`)
- **WebUI streaming error handling**: Partial responses are now preserved when a streaming error occurs rather than being discarded. (#23090, `d528444`)
- **WebUI checksum fix**: HF checksum comparison now uses lowercase hash to avoid case mismatch failures. (#23107, `1348f67`)
- **Codex CLI compatibility**: Server now skips unsupported Responses API tools instead of hard-failing, enabling Codex CLI to work against a llama.cpp server endpoint. (#23041, `91e84fe`)
- **Server timing docs**: Documented the `usage` object fields in the server's timing response. (#23110, `6831fe4`)

## Conversion / Build

- **`convert_hf_to_gguf.py` refactor** (major): Split the monolithic conversion script into a `conversion/` directory with per-architecture modules (deepseek, gemma, falcon, etc.), mirroring the `src/models` layout. Significant maintainability improvement. (#17114, `cc7200b`)
- **Qwen3 ASR conversion fix**: Fixed conversion path for Qwen3 ASR models. (#23081, `18d1717`)
- **CI release symlink fix**: Fixed broken symlinks generated during release archive creation. (#23119, `49d1701`)
- **CI release archive fix**: Fixed transform of top-level entries in release archives. (#23080, `769cc93`)

## Multimodal (mtmd)

- **Qwen3A multimodal**: Added chunk support and fixed preprocessing for Qwen3A vision models. (#23073, `72e60f5`)

## Tests / Misc

- **BF16 non-contiguous `MUL_MAT` tests**: Added test coverage for BF16 non-contiguous tensor permutations. (#22689, `cfabeb1`)
- **`llama-eval` AIME 2026**: Added the AIME 2026 dataset to the evaluation harness. (#23058, `d5dc2e0`)
- **Reasoning budget deep-copy fix**: `clone()` on reasoning-budget objects now performs a deep copy instead of a shallow one. (#23095, `ac33f03`)
- **Readme bindings update**: Updated third-party bindings list. (#23063, `7155a49`)

---

**AI policy violations closed today:** 0 PRs closed for improper AI policy usage.
