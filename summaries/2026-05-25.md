# llama.cpp daily summary — 2026-05-25

A quiet Sunday with 8 commits, mostly backend-specific fixes and CI improvements. Notable activity on alternative compute backends (OpenCL, Hexagon, Snapdragon) and a server-side enhancement for speculative decoding. No breaking changes detected.

## CI / Build

- **CI workflows overhauled** — `build-self-hosted.yml`, `build.yml`, and `server-self-hosted.yml` restructured with expanded runner coverage and updated `ci/run.sh` (#23616, `549b9d8`)
- **CMake UI build fixed** — added `-fPIC` to `llama-ui` static lib and renamed host-compiled embed helper to avoid conflicts (#23592, `63248fc`)
- **numpy 2.x compat** — minor fixes in `convert_legacy_llama.py` and `gguf-py/gguf/quants.py` for numpy 2.x API changes (#23571, `5d246a7`)

## API / Server

- **Draft model margin for `fit`** — server now reserves headroom for the draft model when fitting context, preventing silent slot miscalculation under speculative decoding (#23485, `83eebe9`)

## GPU / Compute Backends

- **OpenCL batch profiling** — rewrote event-query loop to batch profiling calls, improving throughput and fixing memory leaks from unreleased profiling events (#23495, `f306111`)
- **Hexagon flash-attn softmax** — applied REPL (reduce-before-exp pipelining) optimization to HTP HMX flash-attn softmax, consistent with the earlier x86/ARM work in #22993 (#23455, `1c0f6db`)

## Snapdragon / Docs

- **Windows toolchain bump** — Snapdragon HSDK updated to v6.6.0.0 in CMake presets, README, setup scripts, and docs (#23552, `cec51c7`)

## Tensor Parallelism

- **TP zero-sized slice fix** — `ggml-backend-meta.cpp` now correctly handles entirely zero-sized per-device tensor slices, preventing potential crashes in edge-case TP configurations (#23525, `fff63b5`)

---

## AI Policy Report

**1 PR closed for improper AI policy usage** in the last 24 hours:

- **#23419** — "POC: Serving remote LLMs" — author self-disclosed the PR was *"completely AI-generated from start to finish"* and acknowledged it did not meet contribution guidelines. Closed unmerged on 2026-05-24.
