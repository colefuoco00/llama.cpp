# llama.cpp daily summary — 2026-04-19

Seven commits landed on upstream/master in the last 24 hours. The day was
dominated by a broad model-layer refactor and a notable CPU context-fitting
improvement. No breaking changes detected, but the bias-tensor rename touches
55 files and may affect downstream forks that patch model internals.

## Core / llama

- **Fit ctx size for CPU-only inference** — dynamically adjust context size
  when running on CPU to avoid OOM; improves usability on memory-constrained
  hosts. (#21568, `fd1c0ec`)

## Model / Refactor

- **Refactor bias tensor variable names** — broad rename across 55 model
  source files plus `llama-model.cpp/h` for consistency; also migrates
  `jina-bert-v2` to use `create_tensor_qkv`. Large diff but purely
  mechanical. (#22079, `4f02d47`)

## API / Server

- **Expose `media_tag` on `/props` endpoint** — server now returns the
  `media_tag` field, making multimodal media-type info available to clients
  via the properties route. (#22028, `9e5647a`)

## Backend / ggml

- **Multi-segment read support in `ggml-backend-meta`** — `get_tensor` can
  now handle tensors that span multiple non-contiguous memory segments,
  fixing correctness issues with split-buffer backends. (#22063, `59accc8`)

## Android

- **Rename `libcommon` → `libllama-common`** — CMakeLists update aligns the
  Android library name with the rest of the build system. (#22076, `23b8cc4`)

## Build / CI

- **Free disk space before ROCm release build** — adds a disk-cleanup step
  in the GitHub Actions release workflow to prevent out-of-space failures on
  ROCm runners. (#22012, `83d58e0`)

## Convert

- **Suppress typing errors in `convert_hf_to_gguf.py`** — annotates five
  type-inconsistent call sites with `# type: ignore` to unblock CI without
  a full type-system refactor. (#22002, `89a5474`)

---

**PRs closed due to improper AI policy usage:** 0
