# llama.cpp daily summary — 2026-05-23

Today's activity (12 commits) was dominated by SYCL backend improvements, including a notable ~4x token-generation speedup on Intel Arc GPUs. Build infrastructure received significant fixes for Apple platforms and cmake install targets. A batch of fixes landed across perplexity, tokenization, and server message parsing.

## SYCL/GPU

- **BF16 DMMV kernel path added** (~4x token-generation speedup on Intel Arc A/B series); fixes out-of-bounds for `ncols % 64 != 0` — #21580 `8cc67ef`
- **MoE prefill throughput improved** via counting-sort-based expert mapping (`O(n_as + n_routed_rows)` vs. prior `O(n_as * n_routed_rows)`) — #23142 `cc9e331`
- **Gated Delta Net K>1 support** added to SYCL backend — #23174 `56f16f2`
- **Level Zero detection centralized** in `ggml_sycl_init`, removing scattered checks — #23097 `bcfd198`

## OpenCL/GPU

- **Adreno MoE kernels generalized** across quantization types (Q4\_0/1, Q5\_0/1, Q4\_K/5\_K/6\_K, MXFP4); adds BF16↔F32 conversion kernel — #23449 `0f3cb3f`

## API/Server

- **Empty assistant message parsing fixed**: server now only parses empty trailing messages when continuing an existing assistant turn, preventing spurious parse errors — #23506 `1acee6b`

## Fixes

- **Perplexity integer overflow** fixed in token count arithmetic — #23496 `ef570f6`
- **HybridDNA tokenizer** fixed: k-mer tokens now marked to avoid BPE collision during vocab construction — #23466 `afcda09`

## Backends

- **ZenDNN Q8\_0 quantization support** added, synced with latest ZenDNN — #23414 `99d4026`

## Build

- **cmake install() targets added** for all `-impl` shared libraries, fixing `llama-server: error while loading shared libraries` on `cmake --install`; Apple/iOS/Android CI build fixes included — #23511 `bbce619`
- **Router app** now only built during standalone cmake builds (not as a subproject) — #23521 `9c92e96`

## Docs

- Granite 4.0/4.1 model support noted in autoparser and function-calling docs — #23404 `95feeab`

---

## AI Policy Report

**6 PRs were closed in the past few days due to improper AI policy usage** (PRs #23362–#23367, all from the same contributor, all explicitly labeled "[AI Policy Violation]" by maintainers). The PRs spanned small fixes across CUDA, server UI, and Dockerfiles, and were closed per the project's AGENTS.md policy prohibiting predominantly AI-generated submissions.

Additionally, merged commit `8cc67ef` lists an AI model as a commit co-author (`Co-Authored-By: Claude Sonnet 4.6`). The commit was accepted; this appears to be a permitted disclosure case where the human contributor retained primary authorship.
