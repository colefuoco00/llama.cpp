# llama.cpp daily summary — 2026-04-25

Moderate day with 12 commits focused on the Hexagon/Snapdragon backend, WebGPU flash attention, server bug fixes, and a structured output parser fix. The headline item is WebGPU gaining flash attention support in browsers that lack subgroup matrix extensions, broadening browser-based inference coverage.

## WebGPU

- **Enable FLASH_ATTN_EXT on browsers without subgroup matrix support** — adds a tile-based flash attention fallback path (new `flash_attn_tile.wgsl` shader); browsers that don't support subgroup matrix ops can now use flash attention via tiled or vec paths. `13d36cf` (#22199)

## Hexagon / Snapdragon

- Add SOLVE_TRI op — HTP-side triangular solve with HVX vectorization and chunked batch processing for better thread utilization. `8bc492e` (#21974)
- Bump HMX frequency to max corner — performance uplift for HMX matrix-multiply ops on Snapdragon devices. `361fe72` (#22334)
- Use DIRID 13 in `libggml-htp.inf` for modern InfVerif compatibility. `f65bc34` (#22306)

## API / Server

- **Fix SWA-full logic** — corrects sliding-window-attention full-context handling in the server context; potential correctness bug for models using SWA. `ffdd983` (#22288)
- Rename debug tags to match `--cache-idle-slots` naming for consistency. `793d0a7` (#22292)

## Parser / Structured Output

- Fix structured output bug — corrects a logic error in `server-common.cpp` that could produce malformed structured responses. `0adede8` (#22302)

## Metal

- Print GPU description on initialization for easier device identification in logs. `15fa3c4` (#22318)

## Build / CI

- Switch Snapdragon CI runner from `ubuntu-latest` to `ubuntu-slim` to reduce image size. `a702f39` (#22303)
- Fix Jinja template warnings under Clang 21. `dc80c52` (#22313)

## Misc

- Minor `ggml.c` coding style cleanup. `e583f3b` (#22308)
- Remove unused header from `common/jinja/caps.cpp`. `017f090` (#22310)

---

## AI Policy Report

**PRs closed for improper AI policy usage: N/A**

The upstream repository (`ggml-org/llama.cpp`) is outside the scope of available GitHub MCP tools (restricted to `am17an/llama.cpp`), so closed-PR data for today cannot be retrieved programmatically. Manual inspection of the upstream repository is required to produce an accurate count.
